# Sentinel Technical UX V40

The fund UI consumes the existing `technicalOverlay` from `/api/holding-market` and presents Sentinel X v6.4 as the direction/forecast engine and MCDX v4.0 as the conviction engine. Momentum Forecast V37 remains a secondary probabilistic model instead of replacing Sentinel's directional targets.
