"use client";
import{useEffect,useMemo,useState}from"react";import type{CSSProperties}from"react";import type{AppLang}from"../page";import styles from"./CommitteeMeetingV10.module.css";import resolution from"./CommitteePortfolioResolution.module.css";
type VoteLabel="STRONG BUY"|"BUY"|"NEUTRAL"|"CAUTION"|"SELL"|"VETO"|"PENDING";type Action="OPEN NEW"|"ADD EXISTING"|"TRIM"|"EXIT"|"HOLD"|"REJECT";type Proposal={ticker:string;action:Action;amount:number;weight:number|null;currentPrice:number|null;targetPrice:number|null;expectedReturn:number|null;conviction:number|null;reason:string;isNew:boolean;source:"CURRENT"|"CARRY FORWARD"};type Ballot={proposal:Proposal;votes:{desk:string;vote:VoteLabel;reason:string}[];buyVotes:number;vetoed:boolean;approved:boolean};
const members=[{desk:"CIO",name:"Alex Morgan",role:"Chief Investment Officer",avatarX:0},{desk:"MACRO",name:"Dr. Sarah Chen",role:"Global Macro Team",avatarX:16.6667},{desk:"RESEARCH",name:"Michael Johnson",role:"Equity Research",avatarX:33.3333},{desk:"RISK",name:"David Kim",role:"Chief Risk Officer",avatarX:50},{desk:"QUANT",name:"Dr. Emily Zhang",role:"Quantitative Research",avatarX:66.6667},{desk:"PORTFOLIO",name:"James Wilson",role:"Portfolio Construction",avatarX:83.3333},{desk:"TREASURY",name:"Lisa Park",role:"Liquidity & Treasury",avatarX:100}];
const finite=(v:unknown)=>{const n=typeof v==="number"?v:Number(v);return Number.isFinite(n)?n:null},usd=(v:number|null)=>v==null?"—":new Intl.NumberFormat("en-US",{style:"currency",currency:"USD",maximumFractionDigits:2}).format(v),pct=(v:number|null)=>v==null?"—":`${v>=0?"+":""}${v.toFixed(1)}%`,text=(v:unknown,f:string)=>typeof v==="string"&&v.trim()?v:f,supportive=(v:VoteLabel)=>v==="STRONG BUY"||v==="BUY",voteClass=(v:VoteLabel)=>v.toLowerCase().replaceAll(" ","");
async function readJson(path:string,init?:RequestInit){const r=await fetch(path,{cache:"no-store",credentials:"same-origin",headers:{Accept:"application/json","Content-Type":"application/json",...(init?.headers??{})},...init});const type=r.headers.get("content-type")??"",raw=await r.text();if(!type.includes("application/json"))throw new Error(`${path} returned ${r.status} ${type||"non-JSON response"}`);let j:any={};try{j=raw?JSON.parse(raw):{}}catch{throw new Error(`${path} returned invalid JSON`)}if(!r.ok)throw new Error(j?.error??`${path} returned ${r.status}`);return j}
function normalizeAction(raw:string,isNew:boolean):Action{const a=raw.toUpperCase();if(a.includes("TRIM")||a.includes("REDUCE"))return"TRIM";if(a.includes("EXIT")||a.includes("SELL"))return"EXIT";if(a.includes("REJECT"))return"REJECT";if(a.includes("HOLD")||a.includes("WATCH"))return"HOLD";if(isNew||a.includes("OPEN")||a.includes("INITIATE"))return"OPEN NEW";return"ADD EXISTING"}
function votesFor(p:Proposal,ctx:any):Ballot{const macro=String(ctx?.macroAction??"").toUpperCase(),reserve=finite(ctx?.deployable)??0,weight=p.weight??0,positive=p.action==="OPEN NEW"||p.action==="ADD EXISTING";const macroVote:VoteLabel=macro.includes("DEFENSIVE")&&positive?"CAUTION":positive?"BUY":"NEUTRAL",researchVote:VoteLabel=(p.conviction??0)>=85?"STRONG BUY":(p.conviction??0)>=70?"BUY":p.action==="HOLD"?"NEUTRAL":p.source==="CARRY FORWARD"?"BUY":"CAUTION",riskVote:VoteLabel=weight>8?"VETO":weight>5?"CAUTION":p.action==="EXIT"?"SELL":"BUY",quantVote:VoteLabel=(p.expectedReturn??0)>=15?"STRONG BUY":(p.expectedReturn??0)>=8?"BUY":p.action==="TRIM"||p.action==="EXIT"?"SELL":p.source==="CARRY FORWARD"?"BUY":"NEUTRAL",portfolioVote:VoteLabel=p.action==="REJECT"?"SELL":p.action==="HOLD"?"NEUTRAL":"BUY",treasuryVote:VoteLabel=positive&&reserve>0&&p.amount>reserve?"VETO":positive?"BUY":"NEUTRAL",prelim=[macroVote,researchVote,riskVote,quantVote,portfolioVote,treasuryVote],cioVote:VoteLabel=prelim.includes("VETO")?"VETO":prelim.filter(supportive).length>=4?"BUY":p.action==="TRIM"||p.action==="EXIT"?"SELL":"CAUTION";const votes=[{desk:"CIO",vote:cioVote,reason:cioVote==="VETO"?"CIO blocks the proposal until the veto condition is resolved.":"CIO reflects the committee majority and portfolio mandate."},{desk:"MACRO",vote:macroVote,reason:text(ctx?.macroReason,"Macro permits selective deployment with controlled sizing.")},{desk:"RESEARCH",vote:researchVote,reason:p.reason},{desk:"RISK",vote:riskVote,reason:riskVote==="VETO"?"Proposed weight exceeds the 8% risk limit.":"Position size remains inside policy."},{desk:"QUANT",vote:quantVote,reason:`Expected return ${pct(p.expectedReturn)} · conviction ${p.conviction??"—"}/100.`},{desk:"PORTFOLIO",vote:portfolioVote,reason:`Portfolio action: ${p.action}.`},{desk:"TREASURY",vote:treasuryVote,reason:positive?`Deployable liquidity ${usd(reserve)}.`:"This action supplies capital or requires no new cash."}];const buyVotes=votes.filter(x=>supportive(x.vote)).length,vetoed=votes.some(x=>x.vote==="VETO"),approved=!vetoed&&(p.action==="TRIM"||p.action==="EXIT"?votes.filter(x=>x.vote==="SELL"||supportive(x.vote)).length>=4:buyVotes>=4);return{proposal:p,votes,buyVotes,vetoed,approved}}
export default function CommitteeMeetingV10({lang}:{lang:AppLang}){const[data,setData]=useState<any>(null),[loading,setLoading]=useState(true),[error,setError]=useState<string|null>(null),[selected,setSelected]=useState<Record<string,boolean>>({}),[submitting,setSubmitting]=useState(false),[execution,setExecution]=useState<any>(null);
useEffect(()=>{let active=true;const endpoints=[["cio","/api/v10/cio"],["allocation","/api/portfolio/opportunity-allocation"],["optimizer","/api/portfolio/optimizer"],["portfolio","/api/portfolio"],["buffer","/api/portfolio/cash-buffer"],["memory","/api/committee-memory?limit=1"]]as const;Promise.allSettled(endpoints.map(([,p])=>readJson(p))).then(results=>{if(!active)return;const next:Record<string,any>={},failures:string[]=[];results.forEach((r,i)=>{const[k,p]=endpoints[i];if(r.status==="fulfilled")next[k]=r.value;else{next[k]={};failures.push(`${p}: ${r.reason instanceof Error?r.reason.message:String(r.reason)}`)}});setData(next);setError(failures.length?`Partial committee data (${failures.length}/${endpoints.length} unavailable): ${failures.join(" · ")}`:null)}).finally(()=>active&&setLoading(false));return()=>{active=false}},[]);
const proposals=useMemo<Proposal[]>(()=>{if(!data)return[];const holdings=Array.isArray(data.portfolio?.holdings)?data.portfolio.holdings:[],held=new Set(holdings.filter((h:any)=>!h.closed_at).map((h:any)=>String(h.ticker??"").toUpperCase())),out:Proposal[]=[];for(const x of Array.isArray(data.allocation?.allocations)?data.allocation.allocations:[]){const ticker=String(x?.ticker??"").toUpperCase(),amount=finite(x?.approvedCapitalUsd)??0;if(!ticker||amount<=0)continue;const isNew=!held.has(ticker);out.push({ticker,action:isNew?"OPEN NEW":"ADD EXISTING",amount,weight:finite(x?.proposedWeightPct),currentPrice:finite(x?.currentPrice),targetPrice:finite(x?.targetPrice),expectedReturn:finite(x?.expectedReturnPct),conviction:finite(x?.conviction),reason:text(x?.thesis,"Research candidate cleared the allocation bridge."),isNew,source:"CURRENT"})}for(const x of Array.isArray(data.optimizer?.proposals)?data.optimizer.proposals:[]){const ticker=String(x?.ticker??"").toUpperCase();if(!ticker||ticker==="LIQUIDITY"||ticker==="OPPORTUNITY PIPELINE")continue;const action=normalizeAction(String(x?.action??"HOLD"),!held.has(ticker));if(action!=="TRIM"&&action!=="EXIT")continue;out.push({ticker,action,amount:finite(x?.capitalUsd)??0,weight:finite(x?.targetWeightPct),currentPrice:finite(x?.currentPrice),targetPrice:finite(x?.targetPrice),expectedReturn:finite(x?.expectedReturnPct),conviction:finite(x?.score),reason:text(x?.reason,"Portfolio risk-control recommendation."),isNew:false,source:"CURRENT"})}const latest=data.memory?.latest,carryAllowed=latest&&!['CLOSED','CANCELLED','SUPERSEDED','EXECUTED_WITHIN_TOLERANCE'].includes(String(latest.status??''));if(carryAllowed)for(const x of Array.isArray(latest?.resolution)?latest.resolution:[]){const ticker=String(x?.ticker??"").toUpperCase(),action=normalizeAction(String(x?.action??"HOLD"),!held.has(ticker));if(!ticker||action==="HOLD"||action==="REJECT")continue;out.push({ticker,action,amount:finite(x?.amount??x?.plannedUsd??x?.amountUsd)??0,weight:finite(x?.weight??x?.targetWeightPct),currentPrice:finite(x?.currentPrice),targetPrice:finite(x?.targetPrice),expectedReturn:finite(x?.expectedReturn??x?.expectedReturnPct),conviction:finite(x?.conviction??x?.score),reason:text(x?.reason,"Carried forward from the previous unresolved committee resolution."),isNew:!held.has(ticker),source:"CARRY FORWARD"})}return Array.from(new Map(out.map(p=>[`${p.action}:${p.ticker}`,p])).values())},[data]);
const deployable=finite(data?.allocation?.portfolio?.deployableCapitalUsd??data?.buffer?.gapValue)??0,context={macroAction:data?.cio?.decisions?.find((x:any)=>String(x?.desk).toUpperCase()==="MACRO")?.action,macroReason:data?.cio?.decisions?.find((x:any)=>String(x?.desk).toUpperCase()==="MACRO")?.reason,deployable},ballots=useMemo(()=>proposals.map(p=>votesFor(p,context)),[proposals,data]),groups=(["OPEN NEW","ADD EXISTING","TRIM","EXIT","REJECT"]as Action[]).map(action=>({action,items:ballots.filter(b=>b.proposal.action===action)})),approved=ballots.filter(b=>b.approved),positive=approved.filter(b=>b.proposal.action==="OPEN NEW"||b.proposal.action==="ADD EXISTING"),sales=approved.filter(b=>b.proposal.action==="TRIM"||b.proposal.action==="EXIT"),required=positive.reduce((s,b)=>s+b.proposal.amount,0),proceeds=sales.reduce((s,b)=>s+b.proposal.amount,0),reserveTicker=(Array.isArray(data?.portfolio?.holdings)?data.portfolio.holdings:[]).find((h:any)=>["SGOV","BIL","SHV"].includes(String(h?.ticker??"").toUpperCase()))?.ticker??"SGOV",committed=required>0,fundingLabel=committed?`TRIM ${String(reserveTicker).toUpperCase()} ${usd(Math.min(deployable,Math.max(0,required-proceeds)))}`:`${String(reserveTicker).toUpperCase()} EXCESS ${usd(deployable)} · NOT COMMITTED`,balance=proceeds+deployable-required;
const toggle=(k:string)=>setSelected(s=>({...s,[k]:!s[k]})),approveAll=()=>setSelected(Object.fromEntries(approved.map(b=>[`${b.proposal.action}:${b.proposal.ticker}`,true]))),selectedBallots=approved.filter(b=>selected[`${b.proposal.action}:${b.proposal.ticker}`]);
async function submitPackage(){if(!selectedBallots.length)return;setSubmitting(true);setExecution(null);setError(null);try{const result=await readJson("/api/portfolio/rebalance-execution",{method:"POST",body:JSON.stringify({humanApproved:true,humanApprovedBy:"portfolio_owner",meetingCode:data?.memory?.latest?.meeting_code,reserveTicker,deployable,portfolioBefore:data?.portfolio??{},macro:context,items:selectedBallots.map(b=>({...b.proposal}))})});setExecution(result)}catch(e:any){setError(e?.message??"Rebalance submission failed")}finally{setSubmitting(false)}}
return <section className={styles.room}><header><div><span>AI COMMITTEE · UNIFIED MEETING STATE · v10.6</span><h2>{lang==="th"?"มติและคำสั่งปรับพอร์ตทั้งชุด":"PORTFOLIO DECISION & EXECUTION PACKAGE"}</h2><p>{lang==="th"?"อนุมัติหลายหุ้นและส่ง Funding, Sell, Buy เป็นแพ็กเกจเดียว":"Approve multiple assets and submit funding, sells and buys as one ordered package."}</p></div><b>{loading?"CONVENING":execution?String(execution.package?.status??"SUBMITTED"):`${approved.length} APPROVED`}</b></header>{error&&<div className={styles.error}>{error}</div>}{execution&&<div className={styles.error} style={{borderColor:"rgba(72,228,167,.45)",color:"#74f0bd",background:"rgba(72,228,167,.08)"}}>Package {execution.package?.packageId} · {execution.package?.status} · {execution.package?.items?.length??0} ordered tickets. {execution.message}</div>}<div className={styles.members}>{members.map(m=><article key={m.desk}><div className={styles.avatar} style={{backgroundPosition:`${m.avatarX}% center`}as CSSProperties}/><strong>{m.name}</strong><span>{m.desk}</span><small>{m.role}</small></article>)}</div><section className={resolution.stats}>{groups.map(g=><div key={g.action}><span>{g.action}</span><strong>{g.items.length}</strong></div>)}<div><span>APPROVED</span><strong>{approved.length}</strong></div></section><section className={resolution.panel}><div className={resolution.head}><div><span>FUNDING PLAN</span><h3>{fundingLabel}</h3><p>{committed?`${usd(required)} allocated to ${positive.map(x=>x.proposal.ticker).join(", ")}. Funding balance ${usd(balance)}.`:"No destination has been approved."}</p></div><div style={{display:"grid",gap:8}}><button onClick={approveAll} disabled={!approved.length}>Select All Approved</button><button onClick={submitPackage} disabled={!selectedBallots.length||submitting}>{submitting?"Submitting…":`Submit Rebalance Package (${selectedBallots.length})`}</button></div></div>{groups.map(g=>g.items.length>0&&<div className={resolution.group}key={g.action}><h4>{g.action} <span>{g.items.length}</span></h4>{g.items.map(b=>{const key=`${b.proposal.action}:${b.proposal.ticker}`;return <article className={resolution.card}key={key}><div className={resolution.main}><div><strong>{b.proposal.ticker}</strong><span>{b.proposal.action} · {b.proposal.source}</span><p>{b.proposal.reason}</p></div><div className={resolution.amount}><b>{b.proposal.amount>0?usd(b.proposal.amount):b.proposal.weight!=null?`${b.proposal.weight.toFixed(1)}%`:"NO CASH"}</b><small>{b.proposal.currentPrice?`@ ${usd(b.proposal.currentPrice)}`:""}</small></div><div className={b.vetoed?resolution.vetoed:b.approved?resolution.approved:resolution.rejected}>{b.vetoed?"VETOED":b.approved?"APPROVED":"REVIEW"}</div></div><div className={resolution.votes}>{b.votes.map(v=><div key={v.desk}><span>{v.desk}</span><b className={resolution[voteClass(v.vote)]??""}>{v.vote}</b><small>{v.reason}</small></div>)}</div><div className={resolution.footer}><span>{b.buyVotes}/7 supportive votes</span>{b.approved&&<label><input type="checkbox"checked={Boolean(selected[key])}onChange={()=>toggle(key)}/> Human approve</label>}</div></article>})}</div>)}{!ballots.length&&<div className={resolution.empty}>No actionable portfolio proposal is ready. {fundingLabel}.</div>}<div className={resolution.group}><h4>MEETING MINUTES</h4><article className={resolution.card}><div className={resolution.main}><div><strong>{data?.memory?.latest?.meeting_code??`IC-${new Date().toISOString().slice(0,10)}`}</strong><span>{ballots.length} actionable proposals · {approved.length} approved · {selectedBallots.length} human selected</span><p>Funding: {fundingLabel}. Destinations: {positive.length?positive.map(x=>`${x.proposal.action} ${x.proposal.ticker} ${usd(x.proposal.amount)}`).join(" · "):"none authorized"}.</p></div><div className={balance<0?resolution.vetoed:resolution.approved}>{balance<0?"FUNDING GAP":"BALANCED"}</div></div></article></div></section></section>}
